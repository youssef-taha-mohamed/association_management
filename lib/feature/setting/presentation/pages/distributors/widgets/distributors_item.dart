import 'package:flutter/material.dart';

class DistributorsItem extends StatelessWidget {
  const DistributorsItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
